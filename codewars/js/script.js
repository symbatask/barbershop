

// function getMiddle(str) {
//     if (str.length % 2 === 0) {
//         return str.charAt(str.length / 2)
//     } else {
//         return str.slice(Math.ceil(str.length / 2), Math.floor(str.length / 2))
//
//     }
// }
// console.log(getMiddle("test"))
// return str.slice(Math.ceil(str.length/2), Math.floor(str.length/2))

const iqTest = (numbers)=>{
    return.
}